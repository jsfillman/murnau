[tool:pytest]
testpaths = tests
addopts = 
    --cov=src
    --cov-report=html
    --cov-report=term-missing
    --cov-report=xml
    --cov-fail-under=75
    -v
python_files = test_*.py
python_classes = Test*
python_functions = test_*
pythonpath = .